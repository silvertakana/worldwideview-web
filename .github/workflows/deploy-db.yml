name: Deploy Database Migrations

on:
  push:
    branches: [main]
    paths:
      - 'supabase/migrations/*.sql'
  workflow_dispatch:

# Least privilege: this job only checks out the repo and runs `supabase db push`.
permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - uses: actions/checkout@v6
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - name: Dry run
        run: supabase db push --db-url "${{ secrets.SUPABASE_DB_URL }}" --dry-run
      - name: Apply migrations
        run: supabase db push --db-url "${{ secrets.SUPABASE_DB_URL }}"
