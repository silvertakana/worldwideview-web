# Set to "true" to show billing UI (pricing CTAs, subscription management, upgrade prompts)
NEXT_PUBLIC_BILLING_ENABLED=false

# URL pattern for instance setup redirects. {subdomain} is replaced with the instance subdomain.
# Production: NEXT_PUBLIC_INSTANCE_URL_PATTERN=https://{subdomain}.cloud-wwv.dev
# Default (no pattern): no setupUrl is returned, form falls through to onCreated()
NEXT_PUBLIC_INSTANCE_URL_PATTERN=

# Demo instance URL for CTA buttons (Try Demo / Try the Demo)
# Production: https://demo.worldwideview.dev
NEXT_PUBLIC_DEMO_URL=https://demo.worldwideview.dev

# Marketplace URL for nav/footer links
# Production: https://marketplace.worldwideview.dev
NEXT_PUBLIC_MARKETPLACE_URL=https://marketplace.worldwideview.dev

# Base URL for legal page links (e.g., /privacy-policy is appended)
# Production: https://worldwideview.dev/legal
NEXT_PUBLIC_LEGAL_URL=https://worldwideview.dev/legal

# Analytics script URL. Empty = no script rendered.
# Production: https://analytics.worldwideview.dev/script.js
NEXT_PUBLIC_ANALYTICS_SCRIPT_URL=https://analytics.worldwideview.dev/script.js

# Operator alerting for billing failures (see src/lib/alerts/notify.ts). Server-side only.
# The two transports are independent: set either one, or both. All-empty = alerting is
# disabled, which is the correct default for a fresh checkout - the app drops anything
# raised and says so at /api/health (alerting.status = "unconfigured").
#
# Generic JSON webhook. Receives { level, title, message, context, service, timestamp }.
# Production: an endpoint on the owner's own alerting service, e.g. https://alerts.example.com/billing
ALERT_WEBHOOK_URL=

# ntfy (https://ntfy.sh). Both values are required together; either one alone is ignored,
# and /api/health reports that half-configured state as "partially-configured".
#
# ON A PUBLIC NTFY SERVER THE TOPIC NAME IS THE ONLY PASSWORD. ntfy.sh has no account
# model on the public instance: anyone who knows or guesses the topic can subscribe to it
# and read every alert posted to it, and these alerts name user ids and Stripe customer
# and subscription ids. A short or guessable topic is a public feed of the hub's billing
# incidents, however private it feels.
#
# Do one of these:
#   - point NTFY_URL at a self-hosted ntfy, where the topic lives behind your own
#     network; or
#   - keep ntfy.sh but make NTFY_TOPIC a long random string (32+ characters from a
#     password manager), which is the only access control a public topic has; or
#   - use ALERT_WEBHOOK_URL instead, and leave these empty.
# ntfy itself supports access control (users and read/write permissions per topic) on a
# self-hosted server: https://docs.ntfy.sh/config/#access-control
#
# Example shape only - generate your own, never reuse this line:
# NTFY_TOPIC=<a long random string, e.g. from `openssl rand -hex 32`>
NTFY_URL=
NTFY_TOPIC=
